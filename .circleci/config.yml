version: 2.1

jobs:
  build:
    docker:
      - image: outpostuniverse/ubuntu-18.04-gcc-sdl2-physfs-circleci
      steps:
        - checkout
        - run: make -k
    macos:
      xcode: "10.0.0"
      steps:
        - checkout
        - run: make -k
